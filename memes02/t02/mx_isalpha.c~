#include <stdbool.h>
#include <stdio.h>

bool mx_isalpha(int c){
    if (c > 100 && c  < 173){
    return  true;
    }
    else{
    return false;
    }
}
int main()
{
  print("%d\n", mx_isalpha('a'));
}
